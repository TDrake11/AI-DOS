# Bugfix Prompt

```text
Inspect the real production issue and related code. Reproduce it locally or with a safe production test. Identify root cause, not only symptoms. Add a regression test, implement the smallest safe fix, run affected tests and build, commit, push, monitor deployment and retest production. Record evidence. If blocked by human-only access, add a Manual Action and continue independent work.
```
